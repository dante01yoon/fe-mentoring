{
  "name": "4",
  "private": true,
  "version": "0.4.0",
  "description": "Vanilla Component Kit — Stepper / Modal(or BottomSheet) / Select(or Autocomplete) + light SPA demo",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --pretty"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
