import { describe, it } from 'vitest';

describe('board ordering', () => {
  it.todo('moves a card within the same column without mutating the input');
  it.todo('moves a card across columns and calculates before/after ids');
  it.todo('keeps stable order when two cards have the same position');
});
