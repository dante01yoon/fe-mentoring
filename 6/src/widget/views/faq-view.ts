/**
 * FAQ view skeleton.
 *
 * 멘티 가이드:
 * - mocks/api-client.ts 의 fetchFaqItems() 결과를 노출하세요.
 * - 로딩 / 빈 상태 / 에러 상태 디자인을 어떻게 처리할지 결정하세요.
 * - back 버튼으로 home 으로 돌아가기를 라우터로 처리하세요.
 */

export type FaqView = {
  root: HTMLElement;
  destroy(): void;
};

export function createFaqView(): FaqView {
  // TODO(student): FAQ 목록 렌더링 + 로딩/에러 상태 처리
  throw new Error('TODO(student): implement createFaqView()');
}
