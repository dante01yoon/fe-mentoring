/**
 * TODO(student):
 * - useSavedViews()로 저장된 뷰 목록을 가져오세요.
 * - 클릭 시 URL state를 saved view의 filters로 복원하세요.
 * - 새 saved view 생성/삭제 흐름을 추가하세요.
 * - 저장 실패가 화면을 깨뜨리지 않게 하세요.
 */
export function SavedViewBar() {
  return (
    <div className="saved-view-bar" aria-label="Saved views">
      <span className="saved-view-bar__hint">저장된 뷰 placeholder — TODO(student)</span>
    </div>
  );
}
